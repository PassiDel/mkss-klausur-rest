{
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "format": "prettier --write .",
    "check": "prettier --check ."
  },
  "dependencies": {
    "hono": "^3.12.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.2.5"
  }
}
